name: act-verify

on: [push, workflow_dispatch]

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          repository: Hayao0819/Kamisato
          ref: 636e0d4ba17cb103c19817dd5a60dce05838fd26
          path: kamisato-src
      - uses: actions/setup-go@v7
        with:
          go-version: "1.x"
      - uses: Hayao0819/Kamisato/actions/install@636e0d4ba17cb103c19817dd5a60dce05838fd26 # master
        with:
          ayaka: "true"
          source_dir: ${{ github.workspace }}/kamisato-src
      - name: Verify ayaka is installed with the container executor
        run: |
          set -e
          which ayaka
          ayaka build --help | grep -- --executor
          echo "install action OK"
